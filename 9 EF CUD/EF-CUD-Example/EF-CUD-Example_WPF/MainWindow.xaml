<Window x:Class="EF_CUD_Example_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EF_CUD_Example_WPF"
        mc:Ignorable="d"
        
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        
        Title="EF - CUD Example" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="txtOrderID" 
         materialDesign:HintAssist.Hint="Order ID"
         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
         Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="5" />
        <Button x:Name="btnSearch" Content="Search" Grid.Row="0" Grid.Column="3" Margin="5" Click="btnSearch_Click"/>

        <DataGrid x:Name="datagridOrderLines" AutoGenerateColumns="False" Grid.Row="1" Grid.Column="0" 
          IsReadOnly="True" 
          Grid.ColumnSpan="3" Margin="5" SelectionChanged="datagridOrderLines_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding OrderID}" Header="Order ID"></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Quantity}" Header="Quantity"></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Product.Name}" Header="Product"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="btnDelete" Content="Delete" Grid.Row="1" Grid.Column="3" Margin="5" VerticalAlignment="Top" Click="btnDelete_Click"/>

        <TextBox x:Name="txtQuantity" 
         materialDesign:HintAssist.Hint="Quantity"
         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
         Grid.Row="2" Grid.Column="0" Margin="5" />
        <ComboBox x:Name="cmbProducts" materialDesign:HintAssist.Hint="Product" IsEditable="true"
          Grid.Row="2" Grid.Column="1" Margin="5" />
        <Button x:Name="btnAdd" Content="Add" Grid.Row="2" Grid.Column="2" Margin="5" Click="btnAdd_Click"/>
        <Button x:Name="btnUpdate" Content="Update" Grid.Row="2" Grid.Column="3" Margin="5" Click="btnUpdate_Click"/>
        <Button x:Name="btnCancel" Content="Cancel" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="4" Margin="5" Click="btnCancel_Click"/>
    </Grid>
</Window>
